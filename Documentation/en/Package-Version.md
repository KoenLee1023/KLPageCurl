# Package Version

`KLPageCurlVersion` exposes the package API version through
`KLPageCurlVersion.current`. It is a public top-level type even though the Swift
symbol graph omits its case-less enum declaration, so this article is its stable
DocC destination.
